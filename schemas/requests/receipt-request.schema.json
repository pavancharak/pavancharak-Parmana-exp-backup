{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.parmana.ai/requests/receipt-request.schema.json",
  "title": "Receipt Request",
  "description": "Request payload for POST /receipt. businessTransactionId is required and must be a valid UUID — both are rejected with a 400, with distinct messages depending on which check fails.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "businessTransactionId"
  ],
  "properties": {
    "businessTransactionId": {
      "type": "string",
      "format": "uuid",
      "description": "Business Transaction to generate a Receipt for. Its Execution Trust Record must already have a successful (VERIFIED) Verification, or the request fails with a 409."
    }
  },
  "examples": [
    {
      "businessTransactionId": "eed2a972-1bf5-4166-8472-761f76fbf1b2"
    }
  ]
}
